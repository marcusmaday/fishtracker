<%= form_for(@fish_type) do |f| %>
  <% if @fish_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fish_type.errors.count, "error") %> prohibited this fish_type from being saved:</h2>

      <ul>
      <% @fish_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :point_value %><br>
    <%= f.number_field :point_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
